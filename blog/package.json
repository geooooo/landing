{
  "name": "my-blog",
  "version": "1.0.0",
  "author": "Yury Kornienko",
  "license": "MIT",
  "description": "My blog site",
  "scripts": {
    "install": "cd ssg_blog_rs && cargo build --release",
    "build": "cd ssg_blog_rs && cargo run --release ../ssg-blog.config.json",
    "dev": "npm run build && cd ../dist/blog && pwd && python3 -m http.server"
  }
}
